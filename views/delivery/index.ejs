<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<!-- CSS only -->
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x"
			crossorigin="anonymous"
		/>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<% include ../components/navbar.ejs %>
		<li class="nav-item">
			<a class="button" href="/logout">Logout</a>
		</li>

		<h1 style="text-align: center">Manage Delivery</h1>
		<div style="margin: 30px">
			<a href="/delivery/add" class="btn-primary">New Delivery</a>
			<% if (messages.success){ %>
			<strong style="color: rgb(30, 151, 19)"><%= messages.success %></strong>
			<% } %>
		</div>

		<div class="card-container">
			<% data.map((item) => { %>

			<div class="card">
				<form action="" class="card-form">
					<div class="card-content">
						<p>Kode Pengiriman: <%= item.id %></p>
						<p>Kode Barang:<%= item.itemId %></p>
						<p>Kode Pengirim: <%= item.customerId %></p>
						<p>Penerima: <%= item.receiver %></p>
						<p>Kode Distributor: <%= item.postmanId %></p>
					</div>
				</form>
				<div>
					<a
						href="/delivery/delete/<%= item.id %>"
						onclick="return confirm('Are you sure, want to delete!')"
						class="btn-type1"
					>
						Delete
					</a>
					<a
						href="/delivery/edit/<%= item.id %>"
						class="btn-type1"
						style="margin-left: 40px"
					>
						Edit
					</a>
				</div>
			</div>
			<% }) %>
		</div>
		<% include ../components/footer.ejs %>
	</body>
</html>
